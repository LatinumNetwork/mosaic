import { StreamlineIcons } from 'src/types';
export type StreamlineIconProps = {
    name: StreamlineIcons;
};
export declare const StreamlineIcon: ({ name }: StreamlineIconProps) => import("react/jsx-runtime").JSX.Element;
